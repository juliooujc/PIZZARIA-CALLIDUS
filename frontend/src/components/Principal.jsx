import React from "react";
import Home from "./Navegacao";


const Principal = () => {
    return (
        <main className="principal">
            <Home />
        </main>
    );
};

export default Principal;